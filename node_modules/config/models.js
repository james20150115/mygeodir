var mongoose = require('mongoose');

var Schema = mongoose.Schema;
var userSchema = mongoose.Schema({ 
	token : String,
	email: String, 
	hashed_password: String, 
	salt : String,
	temp_str:String
});

//mongoose.connect(mlocal);
//var mlocal = 'mongodb://localhost:27017/node-android';

var mMlab  = 'mongodb://james:1234@ds015334.mlab.com:15334/mygeo'
mongoose.connect(mMlab);

module.exports = mongoose.model('users', userSchema);        
